const Footer = () => {
  return (
    <footer className="text-center py-4 backdrop-blur-sm border-t border-slate-300">
      Carta © {new Date().getFullYear()}
    </footer>
  );
};

export default Footer;
