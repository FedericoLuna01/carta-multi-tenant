import LoginForm from "@/components/forms/login-form";

const LoginPage = () => {
  return (
    <section className="backdrop-blur-sm min-h-screen flex justify-center items-center">
      <LoginForm />
    </section>
  );
};

export default LoginPage;
